services:
  ember-pi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ember-pi-runtime
    volumes:
      - ../shared:/workspace
      - ../config:/config
      - ../memory:/memory
    stdin_open: true
    tty: true
    restart: unless-stopped
    environment:
      - TERM=xterm-256color
